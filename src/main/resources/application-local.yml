server:
  port: 8084

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test
    username: root
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update

logging:
  level:
    ma.pfe: DEBUG